#include <bits/stdc++.h>
using namespace std;
#define ll long long

void sks() {
    int k, x;
    cin >> k >> x;

    if(x%2==0){
      cout<<
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while (t--) {
        sks();
    }
    return 0;
}
